{
  "name": "speaking-coach",
  "version": "0.1.0",
  "private": true,
  "description": "AI 英语口语陪练 Web MVP",
  "engines": {
    "node": ">=22 <25",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
